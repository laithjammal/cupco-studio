{
  "name": "@cupco/geometry",
  "version": "0.1.0",
  "description": "Pure, dependency-free cup geometry engine: CupProfile -> frustum -> 3D surface + production fan",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit",
    "emit:fan": "tsx scripts/emit-fan-svg.ts"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "vitest": "^3.0.0",
    "tsx": "^4.19.0"
  }
}
