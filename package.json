{
  "name": "cupco-studio",
  "private": true,
  "version": "0.1.0",
  "description": "Cupco Studio — customer mockup studio + internal production studio on one shared cup-geometry engine",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "test": "npm run test --workspaces --if-present",
    "build": "npm run build --workspaces --if-present",
    "typecheck": "npm run typecheck --workspaces --if-present"
  },
  "engines": {
    "node": ">=20"
  }
}
